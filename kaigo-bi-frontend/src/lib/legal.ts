// ===================================================
// 法務ページ共通の事業者情報
//
// 🔴 未確定の項目は "（未設定）" のままにしてある。
//    公開前に必ず実際の値へ差し替えること。
//    差し替え漏れがあると、利用規約・プライバシーポリシーに
//    「（未設定）」と表示されたまま公開されてしまう。
//
// 特定商取引法に基づく表記は、これらに加えて
// 「販売価格以外の必要料金」「引渡時期」「返品の可否」等の記載が必要。
// 本ファイルの値だけでは足りない。
// ===================================================

/** 事業者情報 */
export const OPERATOR = {
  /** 事業者名（法人名または屋号） */
  name: "（未設定）",
  /** 運営責任者名 */
  representative: "（未設定）",
  /** 所在地 */
  address: "（未設定）",
  /** 電話番号（特商法の表示項目。請求があれば遅滞なく開示する運用も可） */
  phone: "（未設定）",
  /** 問い合わせ先メールアドレス */
  contactEmail: "（未設定）",
  /** 個人情報の相談窓口（未設定なら contactEmail と同じで可） */
  privacyContact: "（未設定）",
  /** 専属的合意管轄裁判所 */
  court: "（未設定）地方裁判所",
} as const;

/** 法務ページの最終改定日 */
export const LAST_UPDATED = "2026年8月3日";

/** サービス名 */
export const SERVICE_NAME = "kaigo-bi";

/**
 * 事業者情報が未設定のまま残っていないか。
 * 公開前チェックや開発時の警告表示に使う。
 */
export function hasUnsetOperatorFields(): boolean {
  return Object.values(OPERATOR).some((v) => v.includes("（未設定）"));
}
