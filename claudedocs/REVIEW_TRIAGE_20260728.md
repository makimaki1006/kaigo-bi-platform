# 全ページレビュー トリアージ（2026-07-28 夜間自律作業）

3体のレビューエージェント（BI群/M&A群/SEO群）が本番を精査。個別レポート:
- `claudedocs/PAGE_REVIEW_BI.md`（17件: 高6/中6/低5）
- `claudedocs/PAGE_REVIEW_MA.md`（19件: 重大4/中6/軽微9）
- `claudedocs/PAGE_REVIEW_SEO.md`（4件: 重大1/軽微3）
- スクショ: `claudedocs/review_shots/`

以下、私(夜間)が対応した分と、朝の判断・人間作業に回した分を仕分ける。

## ✅ 夜間に修正済み（原因確定・安全な修正）

| 問題 | 原因 | 修正 |
|---|---|---|
| DD支援: 常勤比率0.8%/離職率が施設(66.7%)と桁違い | フロントで0-1小数を×100せず%表示・比較・hrScore計算 | due-diligence: 離職率/常勤比率/hrScoreを×100に統一 |
| trends: 「5005年」「222年」等の異常年 / KPI平均事業年数がおかしい | years_in_business にパース失敗の異常値(min -2979/max 1804)が20件混入 | growth_establishment_trend・growth_kpi・dashboard_kpi のSQLに `years_in_business BETWEEN 1 AND 100` を追加 |
| workforce: 離職率トレンドが300%超・くし状 | API が全47都道府県×各年を返すのを、都道府県区別せず年ソートのみで折れ線化＋separation_rate(%)を更に×100 | 年ごとに全国平均へ集約、×100を除去 |
| market: 介護需要トレンドがくし状(利用者数が0→260万を反復) | 同上（全都道府県×年を時系列に混入） | 年ごとに全国合計へ集約 |
| M&Aスクリーニング: ファネル「全法人」が表示件数(LIMIT後)と同値 | funnel の count に items.len() を使用 | 全国ユニーク法人数を別クエリで取得し funnel/total_corps に反映 |

## 🕙 朝に判断が必要（設計判断を伴う・推測で直すべきでない）

| 問題 | 重大度 | なぜ夜間に直さなかったか / 提案 |
|---|---|---|
| M&Aスクリーニング 魅力度スコアが上位100法人 全件100点 | 重大 | スコア式が「施設数×20+従業者/10」で大手が100に張り付く。外部レビュー(1回目)の哲学(断定スコアを出さない)にも反する。**正しい設計判断が要る**。案: スコア廃止して施設数・従業者の生値でソート＋バッジ表示、または「要確認シグナル」同様に点数化しない |
| /cost-estimation: 「推定する」で API 500 | 高 | サーバーエラーの根本原因調査が要る(次の夜間 or 朝に着手可)。運営コスト推定が完全停止 |
| /benchmark: 8軸レーダーがデータ系列を描画しない(API 200) | 高 | チャート描画コンポーネント側。RadarChartのデータ形状不一致の可能性。要コード調査 |
| /market: 施設分布マップが常に「位置情報なし」 | 高 | 緯度経度カラムが未整備。地図機能は実装 or 非表示の判断が要る |
| DD: 加算取得状況プレースホルダ / benchmark等の空データ | 中 | kasan_summary未実装。実装 or 明示ラベルの判断 |
| hiring-weather: 全47県が同一天気🌤 / 求人倍率1.00倍 | 中 | 天気アイコン閾値の設計、求人倍率は全産業平均(データ制約)。閾値調整で改善可 |
| dashboard: サービス種別概要の「総従業者数」全行「-」 | 中 | by-service集計にtotal_staffが無い(既知)。列非表示 or 集計追加 |
| RSCプリフェッチ失敗のコンソールエラー(複数ページ) | 低 | Renderコールドスタート/プリフェッチのタイミング。実害は小(フォールバック動作) |

## 👤 人間側の作業（SEOレビュー重大 + 法務）

| 項目 | 対応者 |
|---|---|
| 利用規約・プライバシーポリシー・**特商法に基づく運営者情報**の実ページ作成 | 要人間判断（法務）。★Stripe有料課金を公開している以上、特商法表記は優先度高。現状フッターは「(準備中)」テキスト |
| og:image の用意（全ページ未設定） | 画像アセットが要る |
| 独自ドメイン + NEXT_PUBLIC_SITE_URL 設定 + Search Console 登録 | 人間側（既知） |
| Stripe本番キー切替 + Resendドメイン認証 | 人間側（既知、課金開始の前提） |

## 良好が確認された点（レビューで問題なし）

- SEO技術基盤: canonical全て本番URL(localhost混入なし)、robots meta/robots.txt/sitemap.xml正常、公開7=index/アプリ=noindex、数値・免責の誠実さ、レスポンシブ、Sidebar非表示、リンク切れなし
- 外部レビュー7項目の修正が全て効いている(要確認シグナル/財務DD状態表示/カバレッジ表示/financial_status)
- /insights は問題なし、workforce/qualityの主要KPIは正常でページ間整合あり
